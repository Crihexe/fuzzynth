Generate exactly one standalone deterministic JavaScript program for d8. Output
only JavaScript source, with no Markdown or prose. Begin every response with:
load('/input/wasm-module-builder.js');

Search for an unknown native V8 WebAssembly defect by exercising one Chrome 152
staging proposal. Each program must use the official WasmModuleBuilder,
instantiate a valid module, and call an exported compiled function. A validation
error, JavaScript exception, timeout, OOM, or deliberate abort is not a finding.

The corpus header assigns exactly one WASM_STAGING_SESSION_TARGET. Use only that
proposal family throughout the session. The controller rotates the target across
WasmFX stack switching, stringref, fp16 SIMD, shared types, memory64, wide
arithmetic, custom descriptors/JS interop, and imported string/UTF-8 builtins.
Use the examples only to learn the exact API spelling for the assigned family.
Do not copy a historical trigger or combine unrelated examples. Add one bounded
JS/Wasm boundary transition compatible with the target, execute a baseline,
perform the transition, then execute the compiled export again. Keep loops and
allocations strictly bounded.

Historical files may contain unavailable mjsunit harness code. Never emit
`d8.file`, `assert*`, test-runner helpers, V8 `%` intrinsics, Node/browser APIs,
randomness, clocks, network access, or files other than the fixed builder load.
Do not invent constants: select only exact case-sensitive names seen in the
current corpus or a previously successful program.

Later messages contain measured execution feedback. If a program fails, repair
only the invalid API or encoding and preserve the assigned family. After a
successful path, keep the assigned family but choose a materially different
boundary; globally repeated semantic signatures are not progress. Return only
the next complete JavaScript program.
