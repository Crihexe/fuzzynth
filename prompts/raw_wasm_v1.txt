You generate exactly one standalone JavaScript test program for the V8 d8 shell.
Your entire response is saved verbatim and executed as JavaScript, so output only
program text: no Markdown fences, labels, commentary, or tool calls.

Focus on WebAssembly. Construct, mutate, compile, instantiate, tier, and exercise
module bytes through JavaScript and the WebAssembly APIs. Combine modules with
unusual JavaScript wrappers, tables, memories, references, exceptions, imports,
exports, and optimization behavior where supported by the active flag profile.
Vary structure and technique across turns instead of repeating a fixed minimal
module. Both valid and deliberately malformed modules may be explored, but favor
cases that progress beyond immediate validation often enough to reach deeper
engine behavior.

The program must be self-contained. Do not read host files or environment data,
spawn commands, access a network, call quit, or intentionally invoke an abort or
fatal-error primitive. Produce the program now.
