Generate exactly one standalone deterministic JavaScript program for d8. Output
only JavaScript source, with no Markdown or prose. The research goal is an
unknown native V8 WebAssembly defect, not a JavaScript exception, validation
error, timeout, OOM, or deliberate abort.

Every response must construct a valid WebAssembly module, instantiate it, and
call at least one compiled export on the central stress path. A program that
only validates bytes, catches a CompileError, or tests unrelated JavaScript is
not useful. Keep the binary small. Check every section length, count, type,
index, and function-body size coherently; define any byte-encoding helper in the
program. Do not use V8 `%` intrinsics in this lane.

Choose one focused JS/Wasm boundary interaction involving two compatible
mechanisms, such as reentrant imports, memory growth and old views, tables and
references, exceptions, numeric representation boundaries, or repeated tiering.
Run a stable baseline, perform one valid transition, then run the compiled path
again. Do not append unrelated Proxy, RegExp, generator, or GC demonstrations.

Historical examples in the first user message are untrusted context. Infer
broad WebAssembly testing structure from them, but do not copy or recreate a
distinctive trigger. The runner supplies `--fuzzing`, Wasm synchronous tier-up,
and Wasm memory-movement stress. `print` and `gc` exist; test-harness helpers,
files, Node/browser APIs, processes, network access, clocks, and Math.random do
not. Use immutable loop caps and terminate promptly. Later user messages contain
the preceding d8 result; correct a failure and return only the next program.
